# 小红书内容分发系统 - 规格文档

## 1. 项目概述

**项目名称**: Little Red Book Content Distributor
**类型**: 全栈Web应用（内容分发系统）
**核心功能**: 将预生成的小红书内容（标题、文本、图片、话题）随机分配给每个访问用户，确保内容不重复
**目标用户**: 小红书运营人员、营销人员

---

## 2. 技术架构

### 前端分离架构
- **用户端** (`/dist/user`) - 用户访问的页面，不可访问管理功能
- **管理后台** (`/admin`) - 内容管理、话题配置、导入功能
- **后端API** (`/api`) - 提供数据接口

### 技术栈
- **后端**: Node.js + Express
- **数据库**: SQLite（轻量、易部署）
- **前端**: 原生HTML/CSS/JS（用户端）+ 管理后台Vue3
- **图片存储**: 本地文件系统

---

## 3. 数据库设计

### 表结构

#### `contents` - 内容表
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增ID |
| title | TEXT NOT NULL | 标题 |
| content | TEXT NOT NULL | 正文内容 |
| image_path | TEXT | 图片路径 |
| topics | TEXT | 话题（JSON数组格式，如["穿搭", "好物推荐"]） |
| status | TEXT DEFAULT 'available' | available/distributed |
| created_at | DATETIME | 创建时间 |
| distributed_at | DATETIME | 分发时间 |
| distributed_to | TEXT | 分发用户标识（IP/UUID） |

#### `topics_config` - 话题配置表
| 字段 | 类型 | 描述 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增ID |
| name | TEXT NOT NULL | 话题名称（不含#） |
| count | INTEGER DEFAULT 1 | 该话题被选中的权重/数量 |
| enabled | INTEGER DEFAULT 1 | 是否启用 |

#### `admin_config` - 系统配置表
| 字段 | 类型 | 描述 |
|------|------|------|
| key | TEXT PRIMARY KEY | 配置键 |
| value | TEXT | 配置值 |

---

## 4. 功能模块

### 4.1 用户端功能

#### 内容分发
- 用户访问时生成唯一标识（基于IP + 时间戳的UUID）
- 从内容池中随机获取一个 status='available' 的内容
- 将内容状态更新为 'distributed'
- 记录分发信息
- **同一个用户刷新页面仍显示相同内容**

#### 内容展示
- 标题区域（带复制按钮）
- 正文区域（带复制按钮）
- 图片展示（可点击查看大图）
- 话题区域（多个话题，每个带复制按钮，格式：`#话题名#`）

#### 复制功能
- 每个元素（标题、正文、图片、话题）都有独立的复制按钮
- 复制成功显示 toast 提示
- 图片复制：复制图片URL或下载图片

### 4.2 管理后台功能

#### 内容管理
- 查看所有内容列表
- 查看内容状态（可用/已分发）
- 删除内容
- 批量删除功能

#### 内容导入
- 支持 CSV 格式导入
- 支持 JSON 格式导入
- 支持 Excel 格式导入（xlsx）
- 导入时自动验证数据格式
- 显示导入结果（成功数量、失败数量、失败原因）
- 导入模板下载

#### 话题配置
- 添加/编辑/删除话题
- 设置话题数量（从内容池中选取的话题数量）
- 话题格式自动加 `#`
- 批量导入话题

#### 数据统计
- 总内容数
- 已分发内容数
- 可用内容数

---

## 5. API 设计

### 用户端API

#### `GET /api/content/assign`
获取一个可用内容并分配给当前用户
- 响应：{ success: true, content: { title, content, image_url, topics } }
- 无可用内容时：{ success: false, message: "暂无内容" }

#### `GET /api/content/user/:userId`
获取指定用户的内容（用于刷新页面时恢复）
- 响应：{ success: true, content: {...} }

### 管理后台API

#### `GET /api/admin/contents`
获取所有内容列表
- 支持分页、状态筛选

#### `POST /api/admin/contents`
创建单个内容

#### `DELETE /api/admin/contents/:id`
删除单个内容

#### `POST /api/admin/contents/import`
批量导入内容
- 支持 CSV、JSON、XLSX
- 返回：{ success: true, imported: 10, failed: 2, errors: [...] }

#### `GET /api/admin/topics`
获取话题配置

#### `POST /api/admin/topics`
添加话题

#### `PUT /api/admin/topics/:id`
更新话题

#### `DELETE /api/admin/topics/:id`
删除话题

#### `PUT /api/admin/config/topics-count`
设置每个内容的话题数量

#### `GET /api/admin/stats`
获取统计数据

---

## 6. 用户端页面设计

### 视觉风格
- 简约现代，粉色系主题（小红书风格）
- 卡片式布局展示内容
- 毛玻璃效果背景
- 流畅的动画过渡

### 布局结构
```
┌─────────────────────────────────┐
│         [Logo] 小红书内容        │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │      [图片展示]          │    │
│  │      [复制图片]          │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │  标题                    │    │
│  │  [复制标题]              │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │  正文内容...             │    │
│  │  [复制正文]              │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │  #话题1# #话题2# #话题3# │    │
│  │  [复制所有话题]          │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

### 复制按钮样式
- 每个区域独立的复制按钮
- 图标按钮，悬停显示提示
- 点击后显示 "已复制" 反馈

---

## 7. 管理后台设计

### 登录验证
- 简单的管理员密码验证
- 密码存储为哈希值

### 页面结构
- 侧边导航栏
- 主内容区

### 功能区域
1. **内容管理**
   - 内容列表（分页显示）
   - 搜索功能
   - 状态筛选
   - 批量选择删除

2. **内容导入**
   - 文件上传区域
   - 格式选择
   - 预览导入数据
   - 导入结果展示

3. **话题管理**
   - 话题列表
   - 话题数量配置滑块
   - 添加/编辑表单

4. **数据统计**
   - 统计卡片
   - 可视化图表

---

## 8. 安全考虑

- 管理后台需登录才可访问
- 用户端与管理端完全分离
- 图片存储在服务器本地，不暴露路径
- API 使用 CORS 限制

---

## 9. 文件结构

```
/project
├── server.js              # Express服务器
├── database.js            # 数据库操作
├── package.json
├── /public
│   ├── /user              # 用户端页面
│   │   ├── index.html
│   │   ├── styles.css
│   │   └── app.js
│   └── /admin            # 管理后台（后续实现）
│       └── index.html
├── /uploads               # 图片存储目录
├── /data                  # SQLite数据库目录
└── /imports               # 导入文件目录
```

---

## 10. 导入格式说明

### CSV格式
```csv
title,content,image_name,topics
"标题1","正文内容1","image1.jpg","穿搭,好物分享,今日穿搭"
"标题2","正文内容2","image2.jpg","美妆,护肤,测评"
```

### JSON格式
```json
[
  {
    "title": "标题1",
    "content": "正文内容1",
    "image": "image1.jpg",
    "topics": ["穿搭", "好物分享"]
  }
]
```

---

## 11. 话题配置

- 默认话题数量：3
- 可配置范围：1-10
- 话题格式自动处理：输入"穿搭" → 显示"#穿搭#"
- 话题从 topics_config 表随机选取

---

## 12. 验收标准

1. ✅ 用户访问生成唯一内容，刷新不更换
2. ✅ 每个元素（标题/正文/图片/话题）有独立复制按钮
3. ✅ 话题带#格式，数量可配置
4. ✅ 支持CSV/JSON/XLSX批量导入
5. ✅ 内容不重复分发
6. ✅ 用户端与管理后台分离
7. ✅ 管理后台需验证才可访问